# Django Admin Superuser (created automatically)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=adminpassword
DJANGO_SUPERUSER_EMAIL=admin@example.com

# Django Settings
SECRET_KEY="django-insecure-dev-key-only-for-testing-please-change-in-production"
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# Database Settings (PostgreSQL)
DB_NAME=videoflix_db
DB_USER=videoflix_user
DB_PASSWORD=supersecretpassword
DB_HOST=db
DB_PORT=5432

# Redis Cache Settings
REDIS_HOST=redis
REDIS_LOCATION=redis://redis:6379/1
REDIS_PORT=6379
REDIS_DB=0

# Email Settings
# Option 1: Use MailDev for local testing (recommended for development)
# Set USE_MAILDEV=True and keep the settings below as they are
EMAIL_HOST=maildev
EMAIL_PORT=1025
EMAIL_HOST_USER=noreply@videoflix.local
EMAIL_HOST_PASSWORD=not-needed-for-maildev
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=noreply@videoflix.local
USE_MAILDEV=True

# Option 2: Use real SMTP server (for production or real email testing)
# Set USE_MAILDEV=False and configure your email provider below:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# DEFAULT_FROM_EMAIL=your-email@gmail.com
# USE_MAILDEV=False

# Frontend Settings
FRONTEND_URL=http://localhost:5500
FRONTEND_PATH_PREFIX=
BACKEND_URL=http://localhost:8000

